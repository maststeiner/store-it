# oasdiff --err-ignore list — acknowledged breaking changes (ADR-006 / KAIFe G2).
#
# Policy (PO decision, Marcel Steiner): while the API has no released version
# (pre-v1, current state), intentional breaking changes are acceptable and are
# recorded here per change rather than blocking the contract gate. Each entry is
# an exact match against oasdiff's singleline output; unlisted breaking changes
# still fail the gate. Revisit this policy once a version is published.
#
# SPEC-002 (issue #7): `amount` request `type` tightened from `number, string`
# to `number`. Runtime is unchanged (the API still accepts stringified numbers
# via JsonNumberHandling.AllowReadingFromString); only the documented contract is
# tightened, and the only consumer (our frontend) is migrated in the same effort.
in API POST /api/v1/storages/{storageId}/items the `amount` request property `type` changed from `number, string` to `number`
in API PUT /api/v1/storages/{storageId}/items/{itemId} the `amount` request property `type` changed from `number, string` to `number`
