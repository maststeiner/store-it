# node_modules is reinstalled from the lockfile inside the image; copying the host's copy
# would be slow and could smuggle in platform-specific binaries.
node_modules/
dist/
.angular/
coverage/
playwright-report/
test-results/
**/.vs/
**/.idea/
