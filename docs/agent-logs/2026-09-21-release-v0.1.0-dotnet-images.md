# Agent Run Log: first release attempt (v0.1.0) fails — .NET base images at 11.0

> **Date:** 2026-09-21
> **Spec:** none — bug fix, issue [#155](https://github.com/maststeiner/store-it/issues/155) is the frozen G1 input (tech-debt/bug class, no spec)
> **Persona(s):** developer
> **Model:** Claude Fable 5.1
> **Branch / PR:** `fix/dotnet-10-images` → PR linked from #155

---

## Task

Marcel merged the release PR #152 and pushed the annotated tag `v0.1.0`. Watch the first run of
`release.yml`, and fix whatever it exposes.

## Plan

1. Watch run 35649150975; on failure read the failing job's log.
2. Trace the cause through git history; fix minimally; verify the fix by building all three
   images locally on arm64 (the platform that failed); open the fix PR against `develop`.
3. Record the detection gap as its own issue rather than widening this fix.

## Key Decisions

- **Pin the .NET images to `10.0`, do not bump the TargetFramework to 11.** The tech stack is
  .NET 10 LTS by decision; 11.0 is a release candidate. The base images follow the target
  framework, not the other way round.
- **Renovate hold (`allowedVersions: <11.0`) for `dotnet/sdk` and `dotnet/aspnet`**, in the same
  style as the existing holds for `Microsoft.OpenApi` and `typescript`, pointing at #62 (the .NET
  11 move) as the place where it is lifted. The bump had come through #130 — a *major* Renovate PR,
  merged by a human on 2026-09-10 with a green CI, because nothing in CI builds the Dockerfiles.
- **The detection gap is a separate decision (#156).** SPEC-004 decision 7 kept the container
  stack out of CI on purpose; with SPEC-005 the same Dockerfiles produce release images, so that
  decision deserves a revisit — by Marcel, not by this fix.
- **`v0.1.0` stays.** Release tags are protected (ADR-007, immutable); the fix ships as `v0.1.1`.
  The SPEC-005 verification table will name the failed and the succeeding run.

## Observations worth keeping

- The release workflow behaved as designed under failure: arm64 failed at `dotnet publish`,
  fail-fast cancelled amd64, the manifest job was skipped, **nothing was published under the
  version tag** (SPEC-005 AC-06 held on its first real exercise).
- The error surfaces in the build-time OpenAPI generator (`Microsoft.Extensions.ApiDescription.Server`
  runs the API on the SDK image's runtime), so it appears even before the runtime image is touched.
- #152 was rebase-merged, not merge-committed as the PR asked. `main` and `develop` now have
  identical trees with disconnected histories; #154 reconnects them (back-merge, to be merged with
  a merge commit). Recorded in a correction comment on #152 together with a second correction: my
  claim that a `0.x` tag leaves the contract gate skipping was wrong — `ci.yml` uses any strict
  `vX.Y.Z` tag on `main` as the baseline, so `/api/v1` is now guarded against breaking changes.
  ADR-007 decisions 2 and 3 disagree on this; resolving that is a human decision.

## Human Interventions

| # | Intervention | Reason |
|---|--------------|--------|
| 1 | "gemergt, tag v0.1.0 ist gepusht" | Started the watch; the run failed within a minute. |

## Outcome

- **Result:** fix PR opened (`backend/Dockerfile` → 10.0 images, Renovate hold, this log); all three
  images built locally on arm64 with the pinned images (see the PR for the build output).
- **Deviations from spec:** n/a.
- **Harness follow-up:** #156 (image-build gate in CI) — awaiting a human decision.
