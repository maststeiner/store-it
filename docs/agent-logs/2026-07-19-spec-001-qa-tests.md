# Agent Run Log: SPEC-001 QA — tests from acceptance criteria

> **Date:** 2026-07-19
> **Spec:** [SPEC-001](../specs/SPEC-001-manage-storage-items.md)
> **Persona(s):** qa (isolated subagent — spec + public contract only, **no access to `backend/src/`**)
> **Model:** Claude Fable 5 (Claude Code subagent)
> **Branch / PR:** `feature/spec-001-manage-storage-items`

---

## Task

Derive the complete test suite for SPEC-001 from the acceptance criteria (black-box): domain unit tests + service tests (HTTP against real PostgreSQL via Podman testcontainers).

## Plan

One test per AC/EC at both levels where applicable; validation paths assert ProblemDetails `errorCode`; iterate until green or a genuine spec deviation is found (assertions never weakened).

## Key Decisions

- Isolation enforced: the agent never read implementation files — only spec, test guidelines, OpenAPI contract, and provided public signatures.
- Podman socket passed via `.runsettings` (sandbox denied env export); CI unaffected.

## Human Interventions

| # | Intervention | Reason |
|---|--------------|--------|
| — | none during the run | |

## Outcome

- **Result:** 74 new tests (31 StorageTests, 7 ItemTests, 9 StorageEndpointsTests, 27 ItemEndpointsTests + helpers). Domain coverage 97.8 % line / 100 % branch.
- **Findings — 2 genuine implementation defects (spec-derived tests failed, assertions kept):**
  1. **Defect A (blocker):** `POST …/items` returned 500 — EF Core tracked new items as *Modified* (domain-generated Guid keys read as "existing") → UPDATE instead of INSERT. Fixed by developer persona: `ValueGeneratedNever()` on Storage/Item ids; migration regenerated.
  2. **Defect B:** unit outside the fixed list returned 500 instead of 400 — enum-binding `BadHttpRequestException` unmapped. Fixed: mapped to 400 ProblemDetails (`request.invalid`).
- **Verification:** full suite green after fixes — 90 tests (45 domain / 9 architecture / 36 service), all coverage gates pass.
- **Deviations from spec:** none remaining.
- **Harness follow-up:** coverage measurement now excludes source-generated framework code (`Microsoft.*`/`System.*` namespaces in own assemblies) — it distorted the Api module to 46 % although all own code was covered.
